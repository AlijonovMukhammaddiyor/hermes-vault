---
date: 2026-07-06
day: Monday
semester: 1
week_in_semester: 1
course_ids:
  - CS250
  - CS270
  - CS301
task_count: 3
active_courses: 3
---

## Assigned

### CS250 — Arrays & Hashing: Hash Map → O(n) Optimization

**Outcome:** `ah.apply` — Use a hash map/set to achieve O(n) where a brute force is O(n²), on an assigned array problem.
**Difficulty:** med | **Scaffold:** independent | **Proof:** a.ah.solve (LeetCode AC + pattern note)

#### Concept Lesson: The Hash Map as an O(1) Lookup Table

The core insight: a hash map turns "find X in a collection" from O(n) scanning into O(1) amortized lookup. In array problems, brute-force solutions almost always involve a nested loop — for each element, scan the rest. A hash map eliminates the inner loop by storing values you've already seen, enabling a single pass.

**Mental model:** You're walking through the array. At each step, instead of asking "does the complement exist somewhere ahead?", you ask "have I already seen the complement?" The hash map is your memory of everything you've passed.

**Key invariant for today:** `target - nums[i]` was either seen before (found it) or it wasn't (store `nums[i]` and keep going). This is the **complement lookup pattern** — the most common hash map interview pattern.

**Space-for-time trade:** You spend O(n) memory on the hash map to buy O(n) time instead of O(n²). In interview problems, this is almost always the right trade — memory is cheaper than CPU cycles.

**Python hash map operations:**
```python
seen = {}                    # dict is a hash map
seen[key] = value            # O(1) insert/update
if key in seen: ...          # O(1) membership
val = seen.get(key, default) # O(1) safe lookup
seen = set()                 # for presence-only tracking
```

**When to reach for a hash map:**
- "Find two numbers that sum to X" → complement lookup
- "Check if element exists" → O(1) membership
- "Count frequencies" → `collections.Counter` (hash map under the hood)
- "Group by a key" → `defaultdict(list)`
- "First occurrence / earliest index" → store index as value

**Pitfalls:** hash collisions degrade to O(n) worst case (rare with Python's implementation). Don't use a hash map when the problem asks for O(1) space — that means two-pointers or in-place.

#### Task: LeetCode 1 — Two Sum

**Problem:** Given `nums: list[int]` and `target: int`, return indices of two numbers that add to target. Exactly one solution exists.

**Your job:**
1. Solve [Two Sum](https://leetcode.com/problems/two-sum/) on LeetCode with an O(n) hash map solution
2. After AC, write a **3-line pattern note** naming: the pattern (complement lookup), why it applies (we check "have I seen target - nums[i]?"), and the invariant (the map stores {value: index} for all previously seen elements)
3. State the time (O(n)) and space (O(n)) complexity

**Time budget:** ~20 min for the problem (it's Easy — this is baseline calibration for the engine)

---

### CS270 — Foundations: The Augmented LLM

**Outcome:** `fnd.apply` — Build an augmented LLM (prompt + structured output + tool call) and explain each part.
**Difficulty:** med | **Scaffold:** independent | **Proof:** a.fnd.build (grounded note + Rizo GO tie-in)

#### Concept Lesson: The Augmented LLM Architecture

A raw LLM is just text in, text out. An **augmented LLM** adds three layers that make it useful for real work:

```
┌─────────────────────────────────┐
│         Augmented LLM           │
│  ┌───────────────────────────┐  │
│  │ 1. Structured Prompt      │  │  ← system msg + few-shot + context
│  │ 2. Structured Output      │  │  ← JSON schema / function calling
│  │ 3. Tool Integration       │  │  ← LLM decides to call tools, reads results
│  │    ┌────────────────────┐ │  │
│  │    │  Base LLM          │ │  │
│  │    └────────────────────┘ │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

**Layer 1 — Structured Prompt:** The system message sets the persona and constraints. Few-shot examples show the model what good output looks like. The key insight: the prompt is **programming in natural language** — be explicit about format, constraints, and failure modes.

**Layer 2 — Structured Output:** Instead of parsing free-text (fragile), you constrain the LLM to emit valid JSON matching a schema. This is function calling / tool use in reverse — the model's output is machine-readable, enabling reliable pipelines. In practice: use `response_format={"type": "json_object"}` (OpenAI) or equivalent in your stack.

**Layer 3 — Tool Calling:** The LLM doesn't just answer — it decides *when* to call an external function, generates the arguments, and processes the result. This is the bridge from "chatbot" to "agent." The tool definition includes: name, description (critical — this is how the model decides), and parameter schema.

**Why this matters:** Before augmented LLMs, every LLM integration was bespoke string parsing. After: the model emits structured decisions you can act on. This is the atomic building block every agent architecture builds on top of (Anthropic, 2024).

*Source: Anthropic's "Building effective agents" (2024) — "The augmented LLM is the fundamental building block."*

#### Task: Build a Mini Augmented LLM

Build ONE of the following (your choice, grounded in your Rizo GO work):

**Option A (Python script):** Write a Python function that:
1. Takes a user query about Rizo GO data
2. Sends a structured prompt to the LLM
3. The LLM responds with structured JSON (e.g., `{"action": "query_db", "table": "users", "filters": {...}}`)
4. Your code executes the "tool" (simulated — just print what would happen)
5. Returns the result

**Option B (Design doc):** Diagram and explain how you'd add an augmented LLM layer to one specific Rizo GO feature. Cover: what the structured output looks like, what tool(s) the LLM can call, and one failure mode.

**Deliverable:** A note in `Courses/CS270/notes/foundations-augmented-llm.md` with:
- Your implementation or design
- A Feynman teach-back (explain the augmented LLM concept to a junior dev in ≤5 sentences)
- A concrete Rizo GO tie-in

**Time budget:** ~45 min

---

### CS301 — System Design: The 6-Step Delivery Framework

**Outcome:** `fnd.apply` — Apply the 6-step delivery framework (requirements→entities→API→HLD→deep dive) to scope a prompt in time.
**Difficulty:** med | **Scaffold:** worked-completion | **Proof:** a.fnd.framework (rubric-graded design doc)

#### Concept Lesson: The Hello-Interview Delivery Framework

System design interviews are a **performance**, not a lecture. The 6-step framework (from Hello-Interview) gives you a reliable structure that signals senior-level thinking in 45 minutes:

```
 1. Requirements    (5 min)  — clarify functional + non-functional, scope down
 2. Entities        (3 min)  — identify core data objects and their relationships
 3. API Design      (5 min)  — sketch the REST/gRPC surface (just the key endpoints)
 4. High-Level Design (10 min) — boxes-and-arrows diagram of the system
 5. Deep Dive       (15 min) — pick ONE component and go deep (the bottleneck)
 6. Wrap-up         (5 min)  — failure modes, monitoring, what breaks at 10x
```

**Why this order:** Requirements first prevents the #1 interview-killer — building the wrong thing. Entities force you to name your data before you design around it. API grounds the design in something concrete. HLD gives the interviewer a map. The deep dive is where you prove depth. Wrap-up shows operational maturity.

**The secret:** You don't need to design everything. You need to **scope aggressively** in step 1, then go deep on one component in step 5. The interviewer is evaluating your judgment, not your diagram.

**Key signal:** Always start with numbers — "How many users? How many requests per second? How much data?" BOTEC (Back of the Envelope Calculation) belongs in step 1, not as an afterthought. Numbers drive every decision downstream.

*Source: Hello-Interview system design guide — the 6-step delivery framework.*

#### Task: Framework Scoping — URL Shortener

Apply the 6-step framework to design a **URL shortener** (like bit.ly). This is a **worked-completion** task — the scaffolding below provides the structure; you fill in the blanks and defend your choices.

Write your answer in `Courses/CS301/designs/url-shortener-framework.md`.

**Scaffolded template (complete the underlined parts):**

**Step 1 — Requirements (scope down to 3-5 bullets)**
- Functional: users can _________ , the system _________ , optional: _________
- Non-functional: availability target? latency target? read:write ratio?
- BOTEC: assume 100M new URLs/month → ~_________ writes/sec, ~_________ reads/sec (10:1 ratio), storage: _________ GB/year

**Step 2 — Entities**
- URL: (fields: _________, _________, _________, _________)
- User: (fields: _________, _________)  — *decide if you need this*

**Step 3 — API**
- `POST /api/shorten` — body: _________, response: _________
- `GET /{short_code}` — behavior: _________, response: _________

**Step 4 — HLD (draw or describe the boxes)**
- Client → [_________] → [_________] → [_________] → [_________]

**Step 5 — Deep Dive (pick ONE: ID generation, redirect, or analytics)**
- Component: _________
- Challenge: _________
- Solution: _________
- Why this is the bottleneck: _________

**Step 6 — Wrap-up**
- What breaks at 10x traffic? _________
- One failure mode: _________
- One monitoring metric: _________

**Time budget:** ~40 min

---

## Proof

| Course | Outcome | Proof ID | Gate | Deliverable |
|--------|---------|----------|------|-------------|
| CS250 | ah.apply | a.ah.solve | leetcode (two-sum) | AC + 3-line pattern note |
| CS270 | fnd.apply | a.fnd.build | rubric: agents | Grounded note in Courses/CS270/notes/foundations-augmented-llm.md |
| CS301 | fnd.apply | a.fnd.framework | rubric: sysdesign | Completed framework in Courses/CS301/designs/url-shortener-framework.md |

## Log

- 2026-07-06 20:56 Tashkent — DAILY ASSIGN cron run. Semester 1, Week 1, Day 1.
- 3 active courses (CS250 4cr, CS270 2cr, CS301 4cr), 3 tasks assigned, 0 debt.
- CS250 → Two Sum (hash map, independent), engine difficulty: med.
- CS270 → Augmented LLM build (independent), engine difficulty: med.
- CS301 → 6-step framework on URL shortener (worked-completion), engine difficulty: med.
- PD101 inactive (activates week 9).
- Best hours unknown (week 1); defaulting to morning blocks.
