---
date: 2026-07-21
semester: 1
week: 1
phase: foundations
courses: [AG201]
---

## Assigned

### AG201 · U1: The Transformer — Your Agent's Substrate

**Outcome:** u1.transformer (easy · completion)

Agents run on LLMs. You can't design an agent well if you don't know what's happening inside the model — what it sees, what it forgets, and why those things constrain every design decision you'll make for the next 13 weeks. This unit is the foundation.

**Concept — the mental model first.**

A transformer processes a sequence of tokens. Every token "attends" to every other token through three learned projections: **Query** (what am I looking for?), **Key** (what do I contain?), **Value** (what do I contribute if you attend to me?). The dot-product of Q and K tells each token where to look; softmax turns that into weights; multiplying by V produces the attended output. This is *self-attention* — the engine.

**Multi-head** runs several of these in parallel (different subspaces), concatenates, and projects — so the model can attend to syntax, semantics, and position simultaneously. **Residual connections** add the input back to the output of every sublayer (attention + FFN), then **layer norm** stabilizes. Without residuals, deep transformers don't train. **Positional encoding** injects order because attention is permutation-invariant — without it, "dog bites man" and "man bites dog" look identical.

During **inference**, the model generates one token at a time (autoregressive). The KV cache stores already-computed Key and Value projections so you don't recompute them — this is why context windows are expensive (quadratic memory) and why context engineering matters.

**Decoder-only** means no separate encoder — the model reads and generates from the same stack with causal (look-back-only) masking. This design scales better than encoder-decoder, produces more general representations, and GPT-4/Claude/DeepSeek all use it. The **Bitter Lesson** (Sutton, 2019): general methods that scale with compute beat hand-crafted domain knowledge. Decoder-only won because it scales, not because it's conceptually cleaner.

**Why this matters for agents:** your agent sends prompts and tools into a context window. Every design decision — how you structure the system message, when you compact history, how much you offload to external memory — is constrained by attention economics (KV-cache cost) and context-window limits. Master the substrate first.

**Sources:** 3Blue1Brown "Transformers & Attention" (visual intuition) · Karpathy "Deep Dive into LLMs" (~3.5h, full mental model) · Yao "The Second Half" (why we're eval-forward) · Huyen ch. 1–2 (engineering angle)

---

**Your task — Explain the Transformer**

From memory (after watching the resources), produce a self-contained explanation covering:
1. How attention works: Q, K, V, the dot-product, softmax, the weighted sum
2. Why multi-head — what problem does it solve?
3. The full forward pass: token → embedding → positional encoding → N transformer blocks (attention + residual + layer norm → FFN + residual + layer norm) → unembedding → next token
4. Why decoder-only dominates — connect to the Bitter Lesson
5. How the context window constrains agent design (one concrete example)

Format: a **diagram + a paragraph** (or a voice memo if you prefer — reply by voice). No code. No implementation. Conceptual fluency only — you should be able to draw the attention mechanism on a whiteboard and walk someone through it.

**Size:** ~1–1.5h total (45 min watching/reading + 30 min producing your explanation). This is a completion-scaffolded task — the structure above is your guide; fill in the details from the resources.

---

## Proof

_Proof pending — submit via "done" or drag to Done on the board._

## Log

_No entries yet._
